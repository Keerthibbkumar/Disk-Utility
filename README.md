# Disk-Utility
Developed a Python automation script to detect low /boot disk space (&lt;2GB) and remove unnecessary logs, enabling successful security patching across large-scale storage servers and preventing patch failures in regions with thousands of hosts.
