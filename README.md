# Disk-Utility
- Developed a Python automation utility to monitor /boot partition usage and automatically detect low disk space conditions (<2GB) on Linux servers.
- Enabled successful patch deployment across large-scale storage servers, preventing patch failures in regions with thousands of hosts.
- Improved operational efficiency by reducing manual intervention required for disk space management during patch cycles.
